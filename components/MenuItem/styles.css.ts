import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    optionRow: {
        flexDirection: "row",
        marginVertical: 20,
        alignItems: "center"
    },
    optionText: {
        color: "#eee",
        fontSize: 18,
        fontWeight: "bold",
        marginLeft: 10,
    },
    arrowicon: {
        marginLeft: "auto"
    }
})